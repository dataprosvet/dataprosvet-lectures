# Семинар № 11. Математическое ожидание, мода и медиана

> Преподавательская версия: полный пошаговый разбор всех задач.

**Опорная формула:** $E[X]=\sum x_kp_k$ или $\int xf(x)dx$

## Задача S11-P01

Дайте определение объекта «характеристики центра распределения» и укажите, какую информацию он предоставляет.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** Ожидание — взвешенное среднее; мода максимизирует массу/плотность; медиана делит вероятность пополам.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P02

Перечислите условия применимости основной модели семинара.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** для ожидания требуется абсолютная сходимость; мода может быть не единственной
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P03 [типовая]

Для $X=0,1,2$ с вероятностями 0,2; 0,5; 0,3 найдите ожидание.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** $E[X]=0+0{,}5+0{,}6=1{,}1$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P04

Если $P(X=0)=a$, $P(X=2)=1-a$ и $E[X]=1{,}2$, найдите $a$.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** $2(1-a)=1{,}2$, поэтому $a=0{,}4$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P05 [типовая]

Когда медиана информативнее ожидания?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** При асимметрии и тяжёлых хвостах медиана устойчивее к редким экстремальным значениям.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P06

Студент утверждает: «Ожидание обязано быть возможным значением». Найдите ошибку.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** Ожидание — центр в среднем; например, среднее числа детей может быть дробным.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P07

Проверьте размерность, диапазон и предельные случаи формулы $E[X]=\sum x_kp_k$ или $\int xf(x)dx$.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** Вероятность должна лежать в [0,1], параметры — в допустимой области, а единицы результата соответствовать искомой величине. Предельные значения должны согласоваться с механизмом.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P08 [типовая]

По ряду 1,2,3 с массами 0,2;0,5;0,3 найдите моду и медиану.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** Мода 2. Накопленная вероятность достигает 0,7 в точке 2, поэтому медиана 2.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P09

Объясните, как изменение главного параметра модели влияет на вероятность или разброс.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** Нужно удерживать остальные условия постоянными, связать параметр с механизмом и проверить вывод по формуле или монотонности, а не только по интуиции.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P10

Опишите план имитационной проверки одного вычисленного результата.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** Сгенерировать много независимых реализаций строго по модели, вычислить индикатор события или статистику, сравнить частоту с теорией и отдельно обсудить случайную ошибку симуляции.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P11 [типовая]

Что означает $E[L]$ для случайной потери модели?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** Среднюю потерю по генеральному механизму данных при существовании ожидания.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P12

Прибыли -100, 20, 80 имеют вероятности 0,1;0,6;0,3. Найдите ожидаемую прибыль.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** $-10+12+24=26$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P13

Обоснуйте ключевую формулу или переход семинара.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** Линейность следует из вынесения сумм и констант и не требует независимости.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P14 [типовая]

Приведите два распределения с одинаковым ожиданием и разными медианами.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** Например, вырожденное в 1 и распределение 0/2 с равными массами имеют ожидание 1; медиану второго нужно оговорить как любой элемент [0,2] по принятому определению.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S11-P15

Сформулируйте итог для заказчика без формул: что вычислено и при каких ограничениях вывод применим?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «характеристики центра распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: для ожидания требуется абсолютная сходимость; мода может быть не единственной.
3. **Ход решения.** Нужно назвать вероятностный объект «характеристики центра распределения», дать численный или качественный результат, перечислить критические условия (для ожидания требуется абсолютная сходимость; мода может быть не единственной) и не представлять границу или приближение как точную вероятность.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Методические указания

Для каждого решения проверять модель, предпосылки, вычисление, тип вывода и интерпретацию. Старый сценарий сохранён в evidence change и используется как источник кейсов, контрольных формулировок и преподавательских пометок.

## Источники разработки (не для публикации)

- `sources/лекции.pdf` — обязательные границы и порядок тем.
- `sources/Gmurman_V_E_Teoria_veroyatnostey_i_matematicheskaya_statistika.pdf` — классические определения и базовые типы задач.
- `sources/4816 методичка ТВМС.pdf` и `sources/Savyolova_Metodicheskie_ukazaniya_k_resheniyu_zadach_po_veroyatnostnym_razdelam_2014.pdf` — методика решения и контроль предпосылок.
- `sources/математика для машинного обучения.pdf` и `sources/Glubokoe_obuchenie_Pogruzhenie_v_mir_neyronnykh_setey.pdf` — прикладные ML-интерпретации; числовые условия задач переработаны, а не скопированы.

## Связанные материалы

- [Лекция № 5, сценарий](../005_numerical-characteristics.md)
