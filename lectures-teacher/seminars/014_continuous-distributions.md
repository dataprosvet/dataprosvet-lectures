# Семинар № 14. Равномерное, экспоненциальное и распределение Вейбулла

> Преподавательская версия: полный пошаговый разбор всех задач.

**Опорная формула:** $P(a<X\le b)=\int_a^bf(x)dx$

## Задача S14-P01

Дайте определение объекта «непрерывные модели интервала и времени до события» и укажите, какую информацию он предоставляет.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** Равномерный закон имеет постоянную плотность; экспоненциальный — постоянную интенсивность; Вейбулла — гибкую интенсивность отказов.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P02

Перечислите условия применимости основной модели семинара.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** плотность неотрицательна и нормирована; параметры масштаба и формы положительны
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P03 [типовая]

Для $X\sim U(2,6)$ найдите $P(3<X<5)$.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** Отношение длин: $(5-3)/(6-2)=1/2$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P04

Для $X\sim Exp(\lambda)$ известно $E[X]=4$. Найдите $\lambda$.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** $1/\lambda=4$, поэтому $\lambda=0{,}25$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P05 [типовая]

Чем Вейбулла при $k=1$ отличается от общего случая?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** При $k=1$ получается экспоненциальный закон с постоянной интенсивностью; при других $k$ интенсивность меняется.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P06

Студент утверждает: «Отсутствие памяти есть у любого распределения времени». Найдите ошибку.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** Среди непрерывных законов это специальное свойство экспоненциального распределения.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P07

Проверьте размерность, диапазон и предельные случаи формулы $P(a<X\le b)=\int_a^bf(x)dx$.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** Вероятность должна лежать в [0,1], параметры — в допустимой области, а единицы результата соответствовать искомой величине. Предельные значения должны согласоваться с механизмом.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P08 [типовая]

Для $Exp(0{,}5)$ найдите $P(X>4)$.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** $P(X>4)=e^{-0{,}5\cdot4}=e^{-2}$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P09

Объясните, как изменение главного параметра модели влияет на вероятность или разброс.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** Нужно удерживать остальные условия постоянными, связать параметр с механизмом и проверить вывод по формуле или монотонности, а не только по интуиции.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P10

Опишите план имитационной проверки одного вычисленного результата.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** Сгенерировать много независимых реализаций строго по модели, вычислить индикатор события или статистику, сравнить частоту с теорией и отдельно обсудить случайную ошибку симуляции.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P11 [типовая]

Как непрерывный закон можно использовать для времени ответа модели?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** Нужно выбрать поддержку $x\ge0$ и проверить форму хвоста; экспоненциальный закон подходит лишь при постоянной интенсивности.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P12

Как интерпретировать $k>1$ в модели Вейбулла отказов?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** Интенсивность отказа растёт со временем, что соответствует износу.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P13

Обоснуйте ключевую формулу или переход семинара.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** CDF экспоненциального закона получается интегрированием плотности: $1-e^{-\lambda x}$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P14 [типовая]

Сравните модели времени до отказа при постоянной и растущей интенсивности.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** Для постоянной интенсивности выбрать экспоненциальную; для растущей — Weibull с $k>1$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S14-P15

Сформулируйте итог для заказчика без формул: что вычислено и при каких ограничениях вывод применим?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «непрерывные модели интервала и времени до события» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: плотность неотрицательна и нормирована; параметры масштаба и формы положительны.
3. **Ход решения.** Нужно назвать вероятностный объект «непрерывные модели интервала и времени до события», дать численный или качественный результат, перечислить критические условия (плотность неотрицательна и нормирована; параметры масштаба и формы положительны) и не представлять границу или приближение как точную вероятность.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Методические указания

Для каждого решения проверять модель, предпосылки, вычисление, тип вывода и интерпретацию. Старый сценарий сохранён в evidence change и используется как источник кейсов, контрольных формулировок и преподавательских пометок.

## Источники разработки (не для публикации)

- `sources/лекции.pdf` — обязательные границы и порядок тем.
- `sources/Gmurman_V_E_Teoria_veroyatnostey_i_matematicheskaya_statistika.pdf` — классические определения и базовые типы задач.
- `sources/4816 методичка ТВМС.pdf` и `sources/Savyolova_Metodicheskie_ukazaniya_k_resheniyu_zadach_po_veroyatnostnym_razdelam_2014.pdf` — методика решения и контроль предпосылок.
- `sources/математика для машинного обучения.pdf` и `sources/Glubokoe_obuchenie_Pogruzhenie_v_mir_neyronnykh_setey.pdf` — прикладные ML-интерпретации; числовые условия задач переработаны, а не скопированы.

## Связанные материалы

- [Лекция № 6, сценарий](../006_distribution-laws.md)
