# Семинар № 13. Стандартные дискретные распределения

> Преподавательская версия: полный пошаговый разбор всех задач.

**Опорная формула:** формула выбирается после распознавания механизма

## Задача S13-P01

Дайте определение объекта «выбор дискретного закона по механизму» и укажите, какую информацию он предоставляет.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** Биномиальный — успехи в фиксированной независимой серии; Пуассон — счёт событий; геометрический — ожидание первого успеха; гипергеометрический — выбор без возвращения.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P02

Перечислите условия применимости основной модели семинара.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** проверяются поддержка, независимость/возвращение и смысл параметров
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P03 [типовая]

Для $X\sim Pois(3)$ найдите $P(X=0)$.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** $P(X=0)=e^{-3}$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P04

Для $X\sim Bin(20,p)$ известно $E[X]=5$. Найдите $p$.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** $20p=5$, поэтому $p=0{,}25$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P05 [типовая]

Почему выбор без возвращения не биномиален?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** Вероятность успеха меняется после каждого выбора, поэтому испытания зависимы; используется гипергеометрический закон.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P06

Студент утверждает: «Распределение Пуассона имеет параметр-вероятность». Найдите ошибку.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** $\lambda$ — ожидаемое число событий и может быть больше 1.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P07

Проверьте размерность, диапазон и предельные случаи формулы формула выбирается после распознавания механизма.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** Вероятность должна лежать в [0,1], параметры — в допустимой области, а единицы результата соответствовать искомой величине. Предельные значения должны согласоваться с механизмом.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P08 [типовая]

Для геометрического закона с $p=0{,}2$ найдите $P(X=3)$, если $X$ — номер первого успеха.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** $0{,}8^2\cdot0{,}2=0{,}128$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P09

Объясните, как изменение главного параметра модели влияет на вероятность или разброс.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** Нужно удерживать остальные условия постоянными, связать параметр с механизмом и проверить вывод по формуле или монотонности, а не только по интуиции.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P10

Опишите план имитационной проверки одного вычисленного результата.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** Сгенерировать много независимых реализаций строго по модели, вычислить индикатор события или статистику, сравнить частоту с теорией и отдельно обсудить случайную ошибку симуляции.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P11 [типовая]

Какой закон может описывать число ошибок на фиксированном тесте?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** Биномиальный при независимости объектов и одинаковой вероятности ошибки; эти предпосылки нужно проверить.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P12

Какой закон описывает число редких обращений за час при постоянной интенсивности?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** Пуассоновский с параметром, равным ожидаемому числу обращений за час.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P13

Обоснуйте ключевую формулу или переход семинара.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** Гипергеометрическая вероятность — отношение способов выбрать $k$ отмеченных и $n-k$ остальных ко всем выборкам размера $n$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P14 [типовая]

Для одного сюжета сформулируйте версии с возвращением и без возвращения и выберите законы.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** С возвращением вероятность постоянна и возможен биномиальный закон; без возвращения используется гипергеометрический.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S13-P15

Сформулируйте итог для заказчика без формул: что вычислено и при каких ограничениях вывод применим?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «выбор дискретного закона по механизму» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: проверяются поддержка, независимость/возвращение и смысл параметров.
3. **Ход решения.** Нужно назвать вероятностный объект «выбор дискретного закона по механизму», дать численный или качественный результат, перечислить критические условия (проверяются поддержка, независимость/возвращение и смысл параметров) и не представлять границу или приближение как точную вероятность.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Методические указания

Для каждого решения проверять модель, предпосылки, вычисление, тип вывода и интерпретацию. Старый сценарий сохранён в evidence change и используется как источник кейсов, контрольных формулировок и преподавательских пометок.

## Источники разработки (не для публикации)

- `sources/лекции.pdf` — обязательные границы и порядок тем.
- `sources/Gmurman_V_E_Teoria_veroyatnostey_i_matematicheskaya_statistika.pdf` — классические определения и базовые типы задач.
- `sources/4816 методичка ТВМС.pdf` и `sources/Savyolova_Metodicheskie_ukazaniya_k_resheniyu_zadach_po_veroyatnostnym_razdelam_2014.pdf` — методика решения и контроль предпосылок.
- `sources/математика для машинного обучения.pdf` и `sources/Glubokoe_obuchenie_Pogruzhenie_v_mir_neyronnykh_setey.pdf` — прикладные ML-интерпретации; числовые условия задач переработаны, а не скопированы.

## Связанные материалы

- [Лекция № 6, сценарий](../006_distribution-laws.md)
