# Семинар № 12. Дисперсия, моменты, асимметрия и эксцесс

> Преподавательская версия: полный пошаговый разбор всех задач.

**Опорная формула:** $D(X)=E[X^2]-E[X]^2$

## Задача S12-P01

Дайте определение объекта «разброс и форма распределения» и укажите, какую информацию он предоставляет.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** $D(X)=E[(X-E[X])^2]$, $\sigma=\sqrt D$; стандартизованные моменты описывают форму.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P02

Перечислите условия применимости основной модели семинара.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P03 [типовая]

Для $X=0,2$ с равными вероятностями найдите дисперсию.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** $E[X]=1$, $E[X^2]=2$, поэтому $D=2-1=1$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P04

Если $D(3X-2)=36$, найдите $D(X)$.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** $D(3X-2)=9D(X)$, поэтому $D(X)=4$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P05 [типовая]

Сравните дисперсию и стандартное отклонение.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** Дисперсия измеряется в квадрате единиц, стандартное отклонение — в исходных единицах.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P06

Студент утверждает: «$D(aX+b)=aD(X)+b$». Найдите ошибку.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** Сдвиг не влияет на разброс, масштаб входит в квадрате: $D(aX+b)=a^2D(X)$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P07

Проверьте размерность, диапазон и предельные случаи формулы $D(X)=E[X^2]-E[X]^2$.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** Вероятность должна лежать в [0,1], параметры — в допустимой области, а единицы результата соответствовать искомой величине. Предельные значения должны согласоваться с механизмом.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P08 [типовая]

Для симметричного ряда вокруг среднего чему равен третий центральный момент?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** Парные отклонения взаимно сокращаются, поэтому $\mu_3=0$ при существовании момента.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P09

Объясните, как изменение главного параметра модели влияет на вероятность или разброс.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** Нужно удерживать остальные условия постоянными, связать параметр с механизмом и проверить вывод по формуле или монотонности, а не только по интуиции.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P10

Опишите план имитационной проверки одного вычисленного результата.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** Сгенерировать много независимых реализаций строго по модели, вычислить индикатор события или статистику, сравнить частоту с теорией и отдельно обсудить случайную ошибку симуляции.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P11 [типовая]

Почему одной средней потери недостаточно для сравнения моделей?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** Модели с равным средним могут иметь разную дисперсию и вероятность крупных ошибок.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P12

Два независимых риска имеют дисперсии 4 и 9. Найдите дисперсию суммы.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** При независимости ковариация нулевая: $D(X+Y)=4+9=13$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P13

Обоснуйте ключевую формулу или переход семинара.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** Вычислительная формула получается раскрытием квадрата и использованием $E[X-E[X]]=0$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P14 [типовая]

Постройте пример нулевой ковариации без независимости.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** Пусть $X$ симметрична относительно 0, а $Y=X^2$. Тогда $E[X^3]=0$ и ковариация нулевая, но $Y$ полностью определяется $X$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S12-P15

Сформулируйте итог для заказчика без формул: что вычислено и при каких ограничениях вывод применим?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «разброс и форма распределения» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация.
3. **Ход решения.** Нужно назвать вероятностный объект «разброс и форма распределения», дать численный или качественный результат, перечислить критические условия (нужные моменты должны существовать; для суммы зависимых величин учитывается ковариация) и не представлять границу или приближение как точную вероятность.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Методические указания

Для каждого решения проверять модель, предпосылки, вычисление, тип вывода и интерпретацию. Старый сценарий сохранён в evidence change и используется как источник кейсов, контрольных формулировок и преподавательских пометок.

## Источники разработки (не для публикации)

- `sources/лекции.pdf` — обязательные границы и порядок тем.
- `sources/Gmurman_V_E_Teoria_veroyatnostey_i_matematicheskaya_statistika.pdf` — классические определения и базовые типы задач.
- `sources/4816 методичка ТВМС.pdf` и `sources/Savyolova_Metodicheskie_ukazaniya_k_resheniyu_zadach_po_veroyatnostnym_razdelam_2014.pdf` — методика решения и контроль предпосылок.
- `sources/математика для машинного обучения.pdf` и `sources/Glubokoe_obuchenie_Pogruzhenie_v_mir_neyronnykh_setey.pdf` — прикладные ML-интерпретации; числовые условия задач переработаны, а не скопированы.

## Связанные материалы

- [Лекция № 5, сценарий](../005_numerical-characteristics.md)
