# Семинар № 8. Бернулли, Лаплас и Пуассон

> Преподавательская версия: полный пошаговый разбор всех задач.

**Опорная формула:** $P(X=k)=C_n^kp^k(1-p)^{n-k}$

## Задача S08-P01

Дайте определение объекта «число успехов в повторных независимых испытаниях» и укажите, какую информацию он предоставляет.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** Схема Бернулли требует независимости, двух исходов и постоянной вероятности $p$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P02

Перечислите условия применимости основной модели семинара.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P03 [типовая]

При $n=5$, $p=0{,}2$ найдите вероятность ровно одного успеха.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** $C_5^1 0{,}2\cdot0{,}8^4=0{,}4096$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P04

Для биномиальной величины $E[X]=12$ при $n=30$. Найдите $p$.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** $np=12$, поэтому $p=0{,}4$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P05 [типовая]

Когда выбирать Пуассона, а когда интегральную теорему Лапласа?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** Пуассон подходит для редких событий при малом $p$ и умеренном $np$; Лаплас — когда $np$ и $nq$ достаточно велики.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P06

Студент утверждает: «Большое $n$ автоматически разрешает любое приближение». Найдите ошибку.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** Нужно проверять параметры: для нормального приближения важны $np,nq$, для Пуассона — редкость события и $np$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P07

Проверьте размерность, диапазон и предельные случаи формулы $P(X=k)=C_n^kp^k(1-p)^{n-k}$.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** Вероятность должна лежать в [0,1], параметры — в допустимой области, а единицы результата соответствовать искомой величине. Предельные значения должны согласоваться с механизмом.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P08 [типовая]

Для $n=1000$, $p=0{,}002$ вычислите параметр пуассоновского приближения.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** $\lambda=np=2$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P09

Объясните, как изменение главного параметра модели влияет на вероятность или разброс.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** Нужно удерживать остальные условия постоянными, связать параметр с механизмом и проверить вывод по формуле или монотонности, а не только по интуиции.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P10

Опишите план имитационной проверки одного вычисленного результата.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** Сгенерировать много независимых реализаций строго по модели, вычислить индикатор события или статистику, сравнить частоту с теорией и отдельно обсудить случайную ошибку симуляции.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P11 [типовая]

На 10000 объектов вероятность редкой критической ошибки 0,0001. Оцените вероятность ни одной ошибки по Пуассону.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** $\lambda=1$, поэтому $P(X=0)\approx e^{-1}\approx0{,}3679$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P12

В 200 транзакциях вероятность отказа 0,03. Найдите ожидаемое число отказов.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** $E[X]=np=6$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P13

Обоснуйте ключевую формулу или переход семинара.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** Формула Бернулли умножает вероятность одной последовательности с $k$ успехами на число её расположений $C_n^k$.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P14 [типовая]

Для заданных $n,p$ вычислите точный ответ и обоснуйте одно приближение.

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** Сначала использовать биномиальную формулу, затем проверить критерии приближения и сравнить численные значения с указанием характера погрешности.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Задача S08-P15

Сформулируйте итог для заказчика без формул: что вычислено и при каких ограничениях вывод применим?

**Решение.** Полный разбор

1. **Модель.** Определяем объект задачи как «число успехов в повторных независимых испытаниях» и переводим словесное условие в обозначения.
2. **Предпосылки.** Проверяем: фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$.
3. **Ход решения.** Нужно назвать вероятностный объект «число успехов в повторных независимых испытаниях», дать численный или качественный результат, перечислить критические условия (фиксированное $n$, независимость, постоянное $p$; для приближений отдельно проверяются $np$, $nq$ или малость $p$) и не представлять границу или приближение как точную вероятность.
4. **Проверка.** Сверяем область допустимых значений, единицы, граничные случаи и логический статус ответа (точный, оценка, приближение или асимптотика).
5. **Интерпретация.** Ответ относится только к заявленной модели; нарушение хотя бы одной предпосылки требует пересмотра формулы и вывода.

## Методические указания

Для каждого решения проверять модель, предпосылки, вычисление, тип вывода и интерпретацию. Старый сценарий сохранён в evidence change и используется как источник кейсов, контрольных формулировок и преподавательских пометок.

## Источники разработки (не для публикации)

- `sources/лекции.pdf` — обязательные границы и порядок тем.
- `sources/Gmurman_V_E_Teoria_veroyatnostey_i_matematicheskaya_statistika.pdf` — классические определения и базовые типы задач.
- `sources/4816 методичка ТВМС.pdf` и `sources/Savyolova_Metodicheskie_ukazaniya_k_resheniyu_zadach_po_veroyatnostnym_razdelam_2014.pdf` — методика решения и контроль предпосылок.
- `sources/математика для машинного обучения.pdf` и `sources/Glubokoe_obuchenie_Pogruzhenie_v_mir_neyronnykh_setey.pdf` — прикладные ML-интерпретации; числовые условия задач переработаны, а не скопированы.

## Связанные материалы

- [Лекция № 3, сценарий](../003_bernoulli-laplace-poisson.md)
