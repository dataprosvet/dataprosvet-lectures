# Семинар № 2. Комбинаторика и классическое определение вероятности

## Цель и предпосылки

К концу семинара студент различает правило суммы и произведения, размещения, сочетания и перестановки, а затем строит конечное равновозможное пространство исходов и применяет

$$
P(A)=\frac{|A|}{|\Omega|}.
$$

Предполагается знакомство с факториалом, биномиальным коэффициентом и операциями над событиями.

## Маршрут на 90 минут

- 0–10 минут — диагностика моделей и памятка.
- 10–50 минут — типовые `S02-C01`, `S02-C04`, `S02-C10`, `S02-C14`.
- 50–76 минут — типовые `S02-P01`, `S02-P03`, `S02-P10`.
- 76–87 минут — `S02-P23`: аналитика и Python-симуляция балансировки.
- 87–90 минут — итог; остальные 32 задачи выдаются для самостоятельной работы.

## 1. Комбинаторика — 15 задач

### S02-C01 — типовая

**Условие.** Сервис формирует трёхзначный числовой идентификатор из цифр 0, 1, 2, 3, 4, 5 и 9. Сколько идентификаторов возможно: а) если цифры не повторяются; б) если повторения разрешены? Первая цифра не может быть нулём.

**Модель и предпосылки.** Позиции различимы; используем правило произведения и отдельно запрещаем ведущий ноль.

**Решение.** а) Первую цифру выбираем шестью способами, вторую — шестью из оставшихся, третью — пятью: $6\cdot6\cdot5=180$. б) Первая цифра имеет 6 вариантов, остальные — по 7: $6\cdot7\cdot7=294$.

**Проверка.** При разрешении повторов число вариантов не меньше: $294>180$.

**Ответ.** а) 180; б) 294.

**Методический комментарий.** Спросить, почему для второй позиции в пункте а) остаётся шесть цифр: ноль уже разрешён, исключена только использованная первая цифра.

**Источник.** `4816 методичка ТВМС.pdf`, пример 1.2, трансформативная адаптация.

### S02-C02

**Условие.** На первые три урока нужно поставить три разные дисциплины из шести доступных. Сколько расписаний можно составить, если порядок уроков важен?

**Модель и предпосылки.** Выбираются три различных объекта с учётом порядка.

**Решение.** $A_6^3=6\cdot5\cdot4=120$.

**Проверка.** Сначала можно выбрать набор $\binom63=20$ способами, затем упорядочить его $3!=6$ способами; $20\cdot6=120$.

**Ответ.** 120 расписаний.

**Методический комментарий.** Перестановка тех же дисциплин меняет расписание, поэтому сочетания без множителя $3!$ недостаточно.

**Источник.** `4816 методичка ТВМС.pdf`, пример 1.3, адаптация.

### S02-C03

**Условие.** Сколько шестизначных чётных чисел можно получить, использовав каждую из цифр 1, 3, 4, 5, 7, 9 не более одного раза?

**Модель и предпосылки.** Чётность определяется последней цифрой; все шесть цифр различны.

**Решение.** Единственная чётная цифра — 4, значит она обязана стоять последней. Остальные пять цифр переставляются: $5!=120$.

**Проверка.** Ведущего нуля в наборе нет; каждая перестановка задаёт ровно одно допустимое число.

**Ответ.** 120 чисел.

**Методический комментарий.** Удобно сначала фиксировать позицию, на которую наложено самое сильное ограничение.

**Источник.** `4816 методичка ТВМС.pdf`, пример 1.4, адаптация.

### S02-C04 — типовая

**Условие.** Читатель выбирает три разные книги из десяти. Сколько существует наборов, если порядок выбора не учитывается?

**Модель и предпосылки.** Порядок книг в наборе несущественен, повторов нет.

**Решение.** Число сочетаний равно $\binom{10}{3}=10!/(3!7!)=120$.

**Проверка.** Упорядоченных троек $10\cdot9\cdot8=720$; каждый набор посчитан $3!=6$ раз, $720/6=120$.

**Ответ.** 120 наборов.

**Методический комментарий.** Ключевой вопрос студенту: изменится ли результат выбора при перестановке книг?

**Источник.** `4816 методичка ТВМС.pdf`, пример 1.6, адаптация.

### S02-C05

**Условие.** Сколько трёхзначных чётных чисел можно записать цифрами от 0 до 6, если цифры могут повторяться?

**Модель и предпосылки.** Сотни не равны нулю; единицы принадлежат $\{0,2,4,6\}$.

**Решение.** Для сотен 6 вариантов, для десятков 7, для единиц 4: $6\cdot7\cdot4=168$.

**Проверка.** Ограничения на позиции независимы, поскольку повторения разрешены.

**Ответ.** 168 чисел.

**Методический комментарий.** Типичная ошибка — разрешить ноль на первой позиции или забыть ноль среди чётных последних цифр.

**Источник.** `4816 методичка ТВМС.pdf`, пример 1.7, адаптация.

### S02-C06

**Условие.** Код состоит только из символов 4 и 5 и может иметь длину 3, 4 или 5. Сколько разных кодов допустимо?

**Модель и предпосылки.** Для каждой фиксированной длины разрешены повторения; классы длин не пересекаются.

**Решение.** По правилу суммы: $2^3+2^4+2^5=8+16+32=56$.

**Проверка.** Каждая позиция имеет два выбора, а код относится ровно к одной длине.

**Ответ.** 56 кодов.

**Методический комментарий.** Здесь одновременно работают правило произведения внутри длины и правило суммы между длинами.

**Источник.** `4816 методичка ТВМС.pdf`, пример 1.8, адаптация.

### S02-C07

**Условие.** В соревновании участвуют 18 человек. Сколькими способами можно распределить золотую, серебряную и бронзовую медали, если один участник не получает две медали?

**Модель и предпосылки.** Три награды различны, получатели различны.

**Решение.** $A_{18}^3=18\cdot17\cdot16=4896$.

**Проверка.** Выбор тройки и назначение медалей даёт $\binom{18}{3}3!=816\cdot6=4896$.

**Ответ.** 4896 способов.

**Методический комментарий.** Различимость наград превращает сочетание в размещение.

**Источник.** `4816 методичка ТВМС.pdf`, задача 1.4, адаптация.

### S02-C08

**Условие.** Для дежурства выбирают одного сержанта из трёх и трёх солдат из тридцати. Сколько составов дежурной группы возможно?

**Модель и предпосылки.** Выборы из двух непересекающихся категорий независимы; порядок солдат не важен.

**Решение.** $\binom31\binom{30}{3}=3\cdot4060=12180$.

**Проверка.** В каждой группе ровно один сержант и три солдата; двойного счёта нет.

**Ответ.** 12180 составов.

**Методический комментарий.** Произведение используется между обязательными категориями, а не сумма.

**Источник.** `4816 методичка ТВМС.pdf`, задача 1.9, адаптация.

### S02-C09

**Условие.** На матч выбирают одного вратаря из двух, двух защитников из семи и трёх нападающих из десяти. Сколько стартовых составов возможно?

**Модель и предпосылки.** Позиционные группы различны, порядок игроков внутри группы не важен.

**Решение.** $\binom21\binom72\binom{10}{3}=2\cdot21\cdot120=5040$.

**Проверка.** Каждый состав однозначно раскладывается по трём позиционным группам.

**Ответ.** 5040 составов.

**Методический комментарий.** Формула помогает увидеть, какие роли различимы, а какие нет.

**Источник.** `4816 методичка ТВМС.pdf`, задача 1.10, адаптация.

### S02-C10 — типовая

**Условие.** Три приза распределяют между пятью сотрудниками, причём один сотрудник получает не более одного приза. Сколько распределений возможно, если: а) призы различны; б) призы одинаковы?

**Модель и предпосылки.** В пункте а) важны и получатели, и соответствие призов; в пункте б) важен только набор получателей.

**Решение.** а) $A_5^3=5\cdot4\cdot3=60$. б) $\binom53=10$.

**Проверка.** Каждый набор из трёх получателей допускает $3!=6$ назначений разных призов: $10\cdot6=60$.

**Ответ.** а) 60; б) 10.

**Методический комментарий.** Это контрастная пара для различения размещений и сочетаний.

**Источник.** `4816 методичка ТВМС.pdf`, задача 1.15, адаптация.

### S02-C11

**Условие.** Из 30 сотрудников выбирают двух представителей. Сколько вариантов получится: а) если одного из выбранных назначают старшим; б) если роли одинаковы?

**Модель и предпосылки.** В пункте а) после выбора пары назначается одна из двух ролей старшего.

**Решение.** а) $\binom{30}{2}\cdot2=435\cdot2=870$. б) $\binom{30}{2}=435$.

**Проверка.** Эквивалентно в пункте а): 30 способов выбрать старшего и 29 — второго, $30\cdot29=870$.

**Ответ.** а) 870; б) 435.

**Методический комментарий.** Назначенная роль делает пару упорядоченной лишь частично — ровно в два раза.

**Источник.** `4816 методичка ТВМС.pdf`, задача 1.16, адаптация.

### S02-C12

**Условие.** Сколько диагоналей имеет выпуклый двенадцатиугольник?

**Модель и предпосылки.** Любые две вершины задают отрезок; стороны диагоналями не являются.

**Решение.** Всего пар вершин $\binom{12}{2}=66$, из них 12 сторон. Поэтому $66-12=54$.

**Проверка.** Формула $n(n-3)/2$ при $n=12$ также даёт $12\cdot9/2=54$.

**Ответ.** 54 диагонали.

**Методический комментарий.** Вычитание сторон предотвращает смешение всех хорд многоугольника с диагоналями.

**Источник.** `4816 методичка ТВМС.pdf`, задача 1.17, адаптация.

### S02-C13

**Условие.** В круговом турнире каждая пара команд сыграла один матч, всего состоялось 153 матча. Сколько команд участвовало?

**Модель и предпосылки.** Один матч соответствует одной неупорядоченной паре команд.

**Решение.** $\binom n2=153$, то есть $n(n-1)=306$. Уравнение $n^2-n-306=0$ имеет положительный корень $(1+35)/2=18$.

**Проверка.** $18\cdot17/2=153$.

**Ответ.** 18 команд.

**Методический комментарий.** Это обратное применение модели сочетаний.

**Источник.** `4816 методичка ТВМС.pdf`, задача 1.22, адаптация.

### S02-C14 — типовая

**Условие.** Комиссию из восьми человек формируют из двух математиков и восьми экономистов. Сколько комиссий содержат хотя бы одного математика?

**Модель и предпосылки.** Порядок членов комиссии не важен; удобнее считать дополнение.

**Решение.** Всего $\binom{10}{8}=45$ комиссий. Без математиков возможна единственная комиссия из всех восьми экономистов. Искомое число $45-\binom88=44$.

**Проверка.** Прямой счёт: $\binom21\binom87+\binom22\binom86=16+28=44$.

**Ответ.** 44 комиссии.

**Методический комментарий.** Формулировка «хотя бы один» часто делает дополнение короче прямого разбиения.

**Источник.** `4816 методичка ТВМС.pdf`, задача 1.23, адаптация.

### S02-C15

**Условие.** Из 15 рабочих выбирают бригадира и ещё четырёх членов бригады. Сколько различных составов с назначенным бригадиром возможно?

**Модель и предпосылки.** Сначала назначается выделенная роль, затем выбирается неупорядоченная четвёрка из остальных.

**Решение.** $15\binom{14}{4}=15\cdot1001=15015$.

**Проверка.** Можно выбрать пятёрку $\binom{15}{5}=3003$ способами и бригадира внутри неё пятью: $3003\cdot5=15015$.

**Ответ.** 15015 вариантов.

**Методический комментарий.** Оба порядка рассуждения должны давать один результат.

**Источник.** `4816 методичка ТВМС.pdf`, задача 1.24, адаптация.

## 2. Классическое определение вероятности — 25 задач

### S02-P01 — типовая

**Условие.** В контейнере 3 белых и 7 чёрных датчиков. Один датчик выбирают равновероятно. Найдите вероятности выбрать белый и чёрный датчик.

**Модель и предпосылки.** Пространство состоит из 10 конечных равновозможных датчиков.

**Решение.** Для белого события благоприятны 3 исхода, для чёрного — 7: $P(W)=3/10$, $P(B)=7/10$.

**Проверка.** События дополняют друг друга, $3/10+7/10=1$.

**Ответ.** $3/10$ и $7/10$.

**Методический комментарий.** Цвета не обязаны быть равновероятны; равновозможны отдельные датчики.

**Источник.** `4816 методичка ТВМС.pdf`, пример 2.2, адаптация.

### S02-P02

**Условие.** Из пяти разных карточек с буквами А, Б, В, Г, Д случайно без возвращения выкладывают три в ряд. Какова вероятность получить слово «ДВА»?

**Модель и предпосылки.** Равновозможны $A_5^3$ упорядоченных последовательностей.

**Решение.** $|\Omega|=5\cdot4\cdot3=60$; благоприятна одна последовательность. Поэтому $P=1/60$.

**Проверка.** Требуемые буквы есть по одному разу, дополнительных благоприятных перестановок нет.

**Ответ.** $1/60$.

**Методический комментарий.** Порядок извлечения здесь является частью исхода.

**Источник.** `4816 методичка ТВМС.pdf`, пример 2.3, адаптация.

### S02-P03 — типовая

**Условие.** В партии 6 исправных и 4 неисправных устройства. Случайно выбирают два без возвращения. Найдите вероятности событий: а) оба исправны; б) оба неисправны; в) состояния различны.

**Модель и предпосылки.** Равновозможны $\binom{10}{2}=45$ неупорядоченных пары устройств.

**Решение.** а) $\binom62/45=15/45=1/3$. б) $\binom42/45=6/45=2/15$. в) $\binom61\binom41/45=24/45=8/15$.

**Проверка.** События образуют разбиение: $1/3+2/15+8/15=1$.

**Ответ.** а) $1/3$; б) $2/15$; в) $8/15$.

**Методический комментарий.** Подчеркнуть общий знаменатель: выбирается одна равновозможная пара из всех пар.

**Источник.** `4816 методичка ТВМС.pdf`, пример 2.4, адаптация.

### S02-P04

**Условие.** В коробке 10 белых и 5 чёрных шаров. Случайно извлекают один шар. Найдите вероятности событий: а) шар белый; б) шар чёрный; в) шар зелёный.

**Модель и предпосылки.** Конечное пространство из 15 равновозможных шаров.

**Решение.** $P(W)=10/15=2/3$, $P(B)=5/15=1/3$; зелёных шаров нет, поэтому $P(G)=0$.

**Проверка.** Вероятности имеющихся цветов суммируются до 1.

**Ответ.** а) $2/3$; б) $1/3$; в) 0.

**Методический комментарий.** Невозможное событие имеет ноль благоприятных исходов.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.5, адаптация.

### S02-P05

**Условие.** Из стандартной колоды 36 карт случайно выбирают одну. Найдите вероятность получить: а) туза; б) карту достоинства валет, дама, король или туз.

**Модель и предпосылки.** Все 36 карт равновозможны; каждого достоинства по четыре карты.

**Решение.** а) $4/36=1/9$. б) Четыре достоинства по четыре масти дают $16/36=4/9$.

**Проверка.** Тузы включены во второе событие, что согласуется с $1/9<4/9$.

**Ответ.** а) $1/9$; б) $4/9$.

**Методический комментарий.** Перед счётом уточнить состав 36-карточной колоды.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.6, адаптация.

### S02-P06

**Условие.** Из 10 белых и 5 чёрных деталей случайно выбирают две без возвращения. Найдите вероятности событий: а) обе белые; б) обе чёрные; в) цвета различны.

**Модель и предпосылки.** Равновозможны $\binom{15}{2}=105$ пар.

**Решение.** а) $\binom{10}{2}/105=45/105=3/7$. б) $\binom52/105=10/105=2/21$. в) $10\cdot5/105=10/21$.

**Проверка.** $3/7+2/21+10/21=1$.

**Ответ.** а) $3/7$; б) $2/21$; в) $10/21$.

**Методический комментарий.** Три события исчерпывают все цветовые составы пары.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.7, адаптация.

### S02-P07

**Условие.** В урне 5 белых и 4 чёрных шара. Случайно выбирают два без возвращения. Найдите вероятность получить шары: а) одного цвета; б) разных цветов.

**Модель и предпосылки.** Равновозможны $\binom92=36$ пар.

**Решение.** а) $[\binom52+\binom42]/36=(10+6)/36=4/9$. б) $5\cdot4/36=5/9$.

**Проверка.** События взаимно дополняют друг друга: $4/9+5/9=1$.

**Ответ.** а) $4/9$; б) $5/9$.

**Методический комментарий.** Для «одного цвета» требуется правило суммы двух несовместных случаев.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.8, адаптация.

### S02-P08

**Условие.** Из 10 часов шесть нуждаются в чистке. Для проверки случайно выбирают пять часов. Какова вероятность, что ровно двое из выбранных нуждаются в чистке?

**Модель и предпосылки.** Равновозможны все пятёрки; выбираем 2 из 6 требующих чистки и 3 из 4 остальных.

**Решение.** $P=\binom62\binom43/\binom{10}{5}=15\cdot4/252=5/21$.

**Проверка.** Число выбранных по категориям равно пяти, ограничения вместимости соблюдены.

**Ответ.** $5/21$.

**Методический комментарий.** Это гипергеометрическая модель без возвращения.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.9, адаптация.

### S02-P09

**Условие.** В автобусе находятся 4 девушки и 5 юношей. На остановке случайно выходят шесть человек. Какова вероятность, что среди вышедших ровно три девушки и три юноши?

**Модель и предпосылки.** Все $\binom96$ групп равновозможны.

**Решение.** Благоприятных групп $\binom43\binom53=4\cdot10=40$. Поэтому $P=40/84=10/21$.

**Проверка.** Выбираются ровно шесть человек, доступных представителей каждой категории достаточно.

**Ответ.** $10/21$.

**Методический комментарий.** Порядок выхода не учитывается условием.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.10, адаптация.

### S02-P10 — типовая

**Условие.** Бросают два честных шестигранных кубика. Какова вероятность, что сумма будет чётной и не превысит 6?

**Модель и предпосылки.** Равновозможны 36 упорядоченных пар результатов.

**Решение.** Подходят суммы 2, 4 и 6. Им соответствуют 1, 3 и 5 пар, всего 9. Значит, $P=9/36=1/4$.

**Проверка.** Перечень: $(1,1)$; $(1,3),(2,2),(3,1)$; $(1,5),(2,4),(3,3),(4,2),(5,1)$.

**Ответ.** $1/4$.

**Методический комментарий.** Пары $(1,3)$ и $(3,1)$ различны; суммы сами по себе не равновозможны.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.11, адаптация.

### S02-P11

**Условие.** Бросают два честных шестигранных кубика. Какова вероятность получить сумму 6?

**Модель и предпосылки.** Пространство содержит 36 равновозможных упорядоченных пар.

**Решение.** Благоприятны $(1,5),(2,4),(3,3),(4,2),(5,1)$, поэтому $P=5/36$.

**Проверка.** Ни одна координата пары не выходит за диапазон от 1 до 6.

**Ответ.** $5/36$.

**Методический комментарий.** Сравнить с предыдущей задачей и не принимать 11 возможных сумм за равновозможные исходы.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.12, адаптация.

### S02-P12

**Условие.** Пять разных карточек с буквами Т, У, К, Б, Е случайно переставляют. Какова вероятность получить слово «БУКЕТ»?

**Модель и предпосылки.** Все $5!$ перестановок разных физических карточек равновозможны.

**Решение.** Благоприятна одна перестановка, поэтому $P=1/5!=1/120$.

**Проверка.** Все буквы различны, множителей для дублей нет.

**Ответ.** $1/120$.

**Методический комментарий.** Вероятность конкретной перестановки $n$ различных объектов равна $1/n!$.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.13, адаптация.

### S02-P13

**Условие.** Семь карточек несут буквы слова «КОЛОБОК»: К встречается дважды, О — трижды, Л и Б — по одному разу. Карточки случайно выкладывают в ряд. Какова вероятность получить слово «КОЛОБОК»?

**Модель и предпосылки.** Равновозможны $7!$ перестановок физических карточек; одинаковые буквы визуально неразличимы.

**Решение.** На требуемых позициях три карточки О можно переставить $3!$ способами, две К — $2!$ способами. Поэтому $P=3!2!/7!=12/5040=1/420$.

**Проверка.** Число видимых слов равно $7!/(3!2!)=420$, и каждое имеет одинаковое число физических реализаций.

**Ответ.** $1/420$.

**Методический комментарий.** Нужно явно сказать, что случайно переставляются физические карточки.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.14, адаптация.

### S02-P14

**Условие.** В группе 25 студентов: 5 учатся на «отлично», 12 — на «хорошо», остальные имеют другие результаты. Случайно выбирают одного студента. Какова вероятность, что он отличник или хорошист?

**Модель и предпосылки.** 25 студентов равновозможны; категории не пересекаются.

**Решение.** Благоприятны $5+12=17$ студентов, поэтому $P=17/25$.

**Проверка.** $0<17/25<1$; оставшихся студентов 8, и $17/25+8/25=1$.

**Ответ.** $17/25$.

**Методический комментарий.** Для несовместных категорий мощности складываются.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.15, адаптация.

### S02-P15

**Условие.** В коробке 5 красных, 6 жёлтых и 4 зелёных шара. Случайно выбирают один. Какова вероятность, что он жёлтый или зелёный?

**Модель и предпосылки.** Все 15 шаров равновозможны, цветовые события несовместны.

**Решение.** Благоприятны $6+4=10$ шаров, $P=10/15=2/3$.

**Проверка.** Дополнение — красный шар с вероятностью $5/15=1/3$.

**Ответ.** $2/3$.

**Методический комментарий.** Слово «или» здесь означает объединение событий.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.16, адаптация.

### S02-P16

**Условие.** Из колоды 36 карт случайно выбирают одну. Какова вероятность, что это бубновая карта или туз?

**Модель и предпосылки.** Все карты равновозможны; события пересекаются на бубновом тузе.

**Решение.** Бубен 9, тузов 4, общая карта одна. По включению-исключению $P=(9+4-1)/36=12/36=1/3$.

**Проверка.** Без вычитания бубновый туз был бы посчитан дважды.

**Ответ.** $1/3$.

**Методический комментарий.** Сопоставить с `S02-P15`, где пересечения не было.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.17, адаптация.

### S02-P17

**Условие.** Бросают честный шестигранный кубик. Какова вероятность, что результат кратен 2 или 3?

**Модель и предпосылки.** Равновозможны исходы $\{1,2,3,4,5,6\}$.

**Решение.** Подходят $\{2,4,6\}\cup\{3,6\}=\{2,3,4,6\}$, значит $P=4/6=2/3$.

**Проверка.** Число 6 учитывается один раз.

**Ответ.** $2/3$.

**Методический комментарий.** Это простой пример пересечения двух событий.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.18, адаптация.

### S02-P18

**Условие.** Из карточек с буквами слова «ОДЕССА» последовательно выбирают три и записывают по порядку. Какова вероятность получить «САД»: а) если после каждого выбора карточку возвращают; б) если карточки не возвращают?

**Модель и предпосылки.** Всего 6 карточек; С встречается дважды, А и Д — по одному разу. Порядок важен.

**Решение.** а) $P=(2/6)(1/6)(1/6)=1/108$. б) $P=(2/6)(1/5)(1/4)=1/60$.

**Проверка.** После выбора С без возвращения остаются пять, затем после А — четыре карточки.

**Ответ.** а) $1/108$; б) $1/60$.

**Методический комментарий.** Возвращение меняет знаменатели и независимость испытаний.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.19, адаптация.

### S02-P19

**Условие.** Из карточек с буквами слова «КОЛОБОК» последовательно выбирают три и записывают по порядку. Какова вероятность получить «БЛО»: а) с возвращением; б) без возвращения?

**Модель и предпосылки.** Всего 7 карточек: Б и Л по одной, О — три; порядок важен.

**Решение.** а) $P=(1/7)(1/7)(3/7)=3/343$. б) $P=(1/7)(1/6)(3/5)=1/70$.

**Проверка.** В первых двух шагах выбираются единственные Б и Л; без возвращения число карточек убывает.

**Ответ.** а) $3/343$; б) $1/70$.

**Методический комментарий.** Большая вероятность без возвращения здесь не противоречие: после Б и Л доля букв О среди оставшихся растёт.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.20, адаптация.

### S02-P20

**Условие.** Среди 20 фотографий одна отмечена как контрольная. Случайно выбирают четыре разные фотографии. Какова вероятность, что контрольная войдёт в выборку?

**Модель и предпосылки.** Все $\binom{20}{4}$ четвёрки равновозможны.

**Решение.** Благоприятная четвёрка содержит контрольную и ещё три из 19: $P=\binom{19}{3}/\binom{20}{4}=4/20=1/5$.

**Проверка.** По симметрии каждая фотография занимает четыре из двадцати мест выборки в доле $4/20$ всех выборок.

**Ответ.** $1/5$.

**Методический комментарий.** Полезно показать согласие комбинаторного счёта и симметрии.

**Источник.** `4816 методичка ТВМС.pdf`, задача 2.29, адаптация.

### S02-P21

**Условие.** Из целых чисел от 1 до 100 равновероятно выбирают одно. Какова вероятность, что оно делится на 5?

**Модель и предпосылки.** Пространство содержит 100 равновозможных целых чисел.

**Решение.** Кратных пяти $\lfloor100/5\rfloor=20$, поэтому $P=20/100=1/5$.

**Проверка.** Благоприятный ряд: $5,10,\ldots,100$ — арифметическая прогрессия из 20 членов.

**Ответ.** $1/5$.

**Методический комментарий.** Границы диапазона включены.

**Источник.** `Savyolova_Metodicheskie_ukazaniya_k_resheniyu_zadach_po_veroyatnostnym_razdelam_2014.pdf`, пример 1.1, адаптация.

### S02-P22

**Условие.** Три пассажира независимо и равновероятно выходят на одном из четырёх этажей. Какова вероятность, что никакие два пассажира не выйдут на одном этаже?

**Модель и предпосылки.** Пассажиры различимы; равновозможны $4^3$ упорядоченных назначений этажей.

**Решение.** Для первого пассажира 4 выбора, для второго 3 оставшихся, для третьего 2: $P=(4\cdot3\cdot2)/4^3=24/64=3/8$.

**Проверка.** Благоприятное число равно $A_4^3=24$ и не превосходит $|\Omega|=64$.

**Ответ.** $3/8$.

**Методический комментарий.** Этажи выбираются независимо, но событие накладывает запрет на совпадения.

**Источник.** то же пособие, пример 1.2, адаптация.

### S02-P23 — типовая, прикладная

**Условие.** Балансировщик обслуживает 10 серверов. Два сервера выбираются случайно и изначально загружены на 70%, остальные — на 0%. Сравните два механизма: а) каждый запрос направляется на один из 10 серверов равновероятно; б) Power of Two Choices выбирает два различных сервера равновероятно без возвращения и направляет запрос на менее загруженный. Найдите вероятности того, что первый запрос попадёт на один из двух изначально загруженных серверов, подтвердите их Monte Carlo-симуляцией и визуализируйте рост загрузки в Jupyter Notebook.

**Модель и предпосылки.** Начальные серверы равновероятно выбираются один раз и одинаковы для двух алгоритмов. Для первого запроса в пункте а) равновозможны 10 серверов. В пункте б) равновозможны $\binom{10}{2}$ пар; назначение на сервер с начальной загрузкой 70% происходит только при выборе пары из этих двух серверов. В динамической модели один запрос добавляет 5 процентных пунктов, завершение запросов не моделируется, метрика нагрузки доступна без задержки.

**Решение.** Для случайной маршрутизации

$$
P_1=\frac{2}{10}=\frac15.
$$

Для Power of Two Choices

$$
P_2=\frac{\binom22}{\binom{10}{2}}=\frac1{45}.
$$

Для первого назначения отношение вероятностей равно $(1/5)/(1/45)=9$. Ячейки ниже запускаются в Jupyter по порядку.

```python
from math import comb
from random import Random

import matplotlib.pyplot as plt
from matplotlib.animation import FuncAnimation
from matplotlib.patches import Patch, Rectangle
from IPython.display import HTML, display

SERVER_COUNT = 10
INITIAL_LOAD = 0.70
REQUEST_LOAD = 0.05
REQUESTS = 140
TRIALS = 100_000
SEED = 2026

initial_servers = set(Random(SEED).sample(range(SERVER_COUNT), 2))
initial_loads = [
    INITIAL_LOAD if server in initial_servers else 0.0
    for server in range(SERVER_COUNT)
]

print("Серверы с начальной загрузкой 70%:", sorted(initial_servers))
```

Если `matplotlib` отсутствует в kernel, перед первой ячейкой нужно выполнить `%pip install matplotlib` и перезапустить kernel.

```python
monte_carlo_rng = Random(SEED + 1)
random_hits = 0
power_two_hits = 0

for _ in range(TRIALS):
    random_hits += monte_carlo_rng.randrange(SERVER_COUNT) in initial_servers

    candidates = monte_carlo_rng.sample(range(SERVER_COUNT), 2)
    chosen = min(candidates, key=initial_loads.__getitem__)
    power_two_hits += chosen in initial_servers

p_random = random_hits / TRIALS
p_power_two = power_two_hits / TRIALS
exact_random = len(initial_servers) / SERVER_COUNT
exact_power_two = comb(len(initial_servers), 2) / comb(SERVER_COUNT, 2)

print(f"random:    empirical={p_random:.5f}, exact={exact_random:.5f}")
print(f"power-two: empirical={p_power_two:.5f}, exact={exact_power_two:.5f}")

assert abs(p_random - exact_random) <= 0.005
assert abs(p_power_two - exact_power_two) <= 0.005
```

При seed 2026 выбираются серверы 1 и 5; получено `0.19812` против `0.20000` и `0.02253` против `0.02222`. Обе ошибки меньше 0.005.

```python
def simulate_requests(strategy, seed):
    loads = initial_loads.copy()
    history = [loads.copy()]
    dropped_history = [0]
    dropped = 0
    rng = Random(seed)

    for _ in range(REQUESTS):
        if strategy == "random":
            chosen = rng.randrange(SERVER_COUNT)
        elif strategy == "power_two":
            candidates = rng.sample(range(SERVER_COUNT), 2)
            chosen = min(candidates, key=loads.__getitem__)
        else:
            raise ValueError("Unknown strategy")

        if loads[chosen] >= 1.0:
            dropped += 1
        else:
            loads[chosen] = min(1.0, loads[chosen] + REQUEST_LOAD)

        history.append(loads.copy())
        dropped_history.append(dropped)

    return history, dropped_history


random_history, random_dropped = simulate_requests("random", SEED + 2)
power_history, power_dropped = simulate_requests("power_two", SEED + 3)


def load_color(load):
    if load >= 1.0 - 1e-12:
        return "#d62728"  # красный: 100%
    if load >= 0.80 - 1e-12:
        return "#ff7f0e"  # оранжевый: 80–99%
    if load >= 0.50 - 1e-12:
        return "#f2c94c"  # жёлтый: 50–79%
    return "#4c78a8"      # синий: менее 50%


fig, axes = plt.subplots(1, 2, figsize=(13, 5.8))
panels = []

for axis, title in zip(
    axes,
    ("Равномерный случайный выбор", "Power of Two Choices"),
):
    axis.set_xlim(-0.15, 5.0)
    axis.set_ylim(-0.25, 2.25)
    axis.set_aspect("equal")
    axis.axis("off")
    axis.set_title(title, fontsize=13, fontweight="bold")

    fills = []
    percentages = []
    for server in range(SERVER_COUNT):
        x = server % 5
        y = 1 - server // 5
        axis.add_patch(Rectangle((x, y), 0.82, 0.82, fill=False,
                                 edgecolor="#333333", linewidth=1.5))
        fill = Rectangle((x, y), 0.82, 0.0, facecolor="#4c78a8")
        axis.add_patch(fill)
        fills.append(fill)
        percentages.append(
            axis.text(x + 0.41, y + 0.43, "0%", ha="center", va="center",
                      fontsize=10, fontweight="bold")
        )
        axis.text(x + 0.41, y - 0.08, f"S{server}", ha="center", va="top",
                  fontsize=9)

    status = axis.text(2.41, 2.08, "", ha="center", fontsize=10)
    panels.append((fills, percentages, status))

legend = [
    Patch(color="#4c78a8", label="< 50%"),
    Patch(color="#f2c94c", label="50–79%"),
    Patch(color="#ff7f0e", label="80–99%"),
    Patch(color="#d62728", label="100%"),
]
fig.legend(handles=legend, loc="lower center", ncol=4, frameon=False)
heading = fig.suptitle("", fontsize=14)
fig.tight_layout(rect=(0, 0.08, 1, 0.92))


def update(frame):
    heading.set_text(f"Обработано запросов: {frame} из {REQUESTS}")
    states = (
        (random_history[frame], random_dropped[frame]),
        (power_history[frame], power_dropped[frame]),
    )

    for (fills, percentages, status), (loads, dropped) in zip(panels, states):
        for fill, label, load in zip(fills, percentages, loads):
            fill.set_height(0.82 * load)
            fill.set_facecolor(load_color(load))
            label.set_text(f"{load:.0%}")
        status.set_text(f"Отклонено запросов: {dropped}")


animation = FuncAnimation(
    fig,
    update,
    frames=range(REQUESTS + 1),
    interval=120,
    repeat=False,
)
plt.close(fig)
display(HTML(animation.to_jshtml()))
```

**Проверка.** Начальные состояния двух пулов совпадают. При заданных параметрах обычный алгоритм заканчивает загрузками `[90, 100, 60, 80, 65, 100, 55, 80, 75, 85]%` и 10 отклонёнными запросами. Power of Two Choices заканчивает загрузками `[80, 90, 90, 85, 85, 85, 80, 85, 85, 75]%` без отклонений. Цветовая функция отдельно проверяется на значениях 49%, 50%, 79%, 80%, 99% и 100%.

**Ответ.** Для первого запроса $P_1=1/5$, $P_2=1/45$; динамическая модель показывает более равномерное заполнение при Power of Two Choices.

**Методический комментарий.** Остановите анимацию около 50-го и 100-го запроса и попросите сравнить разброс. Это учебная модель: завершение запросов, задержки метрик и неоднородная мощность серверов не учитываются. Точные $1/5$ и $1/45$ нельзя переносить на поздние кадры, потому что загрузки уже изменились.

**Источник.** `Savyolova ... 2014.pdf`, пример 1.3 (общая выборка без возвращения), прикладная трансформативная адаптация.

### S02-P24

**Условие.** Десять разных карточек с буквами слова «ПОРТУГАЛИЯ» случайно переставляют. Какова вероятность получить исходное слово?

**Модель и предпосылки.** Все десять букв различны, равновозможны $10!$ перестановок.

**Решение.** Благоприятна одна перестановка: $P=1/10!=1/3628800$.

**Проверка.** В слове действительно десять букв без повторений.

**Ответ.** $1/3628800$.

**Методический комментарий.** Перед использованием $n!$ нужно проверить повторы букв.

**Источник.** то же пособие, задача 1.9, адаптация.

### S02-P25

**Условие.** Десять физических карточек с буквами слова «МАДАГАСКАР» случайно переставляют; четыре карточки несут букву А. Какова вероятность, что видимая последовательность совпадёт со словом «МАДАГАСКАР»?

**Модель и предпосылки.** Равновозможны $10!$ перестановок физических карточек; четыре карточки А можно менять местами на четырёх требуемых позициях.

**Решение.** Благоприятных физических перестановок $4!$, поэтому $P=4!/10!=24/3628800=1/151200$.

**Проверка.** Число различных видимых слов равно $10!/4!=151200$.

**Ответ.** $1/151200$.

**Методический комментарий.** Отличие от `S02-P24` вызвано повторением буквы А.

**Источник.** то же пособие, задача 1.10, адаптация.

## Памятка преподавателю

- Сначала задайте три вопроса: важен ли порядок, разрешены ли повторы, различимы ли роли.
- Для классической вероятности до формулы потребуйте назвать $\Omega$, доказать его конечность и объяснить равновозможность элементарных исходов.
- Типичные ошибки: ведущий ноль; смешение сочетаний и размещений; двойной счёт пересечения; принятие сумм кубиков за равновозможные; неизменные знаменатели при выборе без возвращения.
- В `S02-P23` отдельно проговорите границы модели: это учебное сравнение двух механизмов, а не обещание девятикратного улучшения в любой реальной системе.

## Непубличная карта источников

- `S02-C01`–`S02-C15`: `4816 методичка ТВМС.pdf`, примеры 1.2, 1.3, 1.4, 1.6, 1.7, 1.8 и задачи 1.4, 1.9, 1.10, 1.15, 1.16, 1.17, 1.22, 1.23, 1.24.
- `S02-P01`–`S02-P20`: то же пособие, примеры 2.2–2.4, задачи 2.5–2.20 и 2.29.
- `S02-P21`–`S02-P25`: `Savyolova_Metodicheskie_ukazaniya_k_resheniyu_zadach_po_veroyatnostnym_razdelam_2014.pdf`, примеры 1.1–1.3 и задачи 1.9–1.10.
- `Savelova_Metod_Monte-Karlo_2011.pdf` не использовалось: содержание не соответствует заявленным темам семинара.
