# Swagger UI assets

Swagger UI Dist **5.32.15**, distributed under Apache-2.0; see [license](SWAGGER-LICENSE).

Upstream files: https://cdn.jsdelivr.net/npm/swagger-ui-dist@5.32.15/swagger-ui-bundle.js and https://cdn.jsdelivr.net/npm/swagger-ui-dist@5.32.15/swagger-ui.css.

The services serve these files locally. Swagger UI does not need an external CDN during the lesson. These are third-party library files, not course-authored code. Versions remain pinned; no moving `latest` URLs are used by the running apps.

Tarball integrity and file SHA-256: [swagger-provenance.json](swagger-provenance.json).
