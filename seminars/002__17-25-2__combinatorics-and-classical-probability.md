# Семинар № 2. Комбинаторика и классическое определение вероятности
> Группа **[17-25-2пг]**
## 1. Комбинаторика — 15 задач

### S02-C01 — типовая

**Условие.** Сервис формирует трёхзначный числовой идентификатор из цифр 0, 1, 2, 3, 4, 5 и 9. Сколько идентификаторов возможно: а) если цифры не повторяются; б) если повторения разрешены? Первая цифра не может быть нулём.

**Разбор.** В пункте а) первую цифру выбираем шестью способами, вторую — шестью из оставшихся, третью — пятью:

$$
6\cdot6\cdot5=180.
$$

В пункте б) первая цифра имеет 6 вариантов, остальные позиции — по 7:

$$
6\cdot7\cdot7=294.
$$

**Ответ.** а) 180; б) 294.

### S02-C02

**Условие.** На первые три урока нужно поставить три разные дисциплины из шести доступных. Сколько расписаний можно составить, если порядок уроков важен?

### S02-C03

**Условие.** Сколько шестизначных чётных чисел можно получить, использовав каждую из цифр 1, 3, 4, 5, 7, 9 не более одного раза?

### S02-C04 — типовая

**Условие.** Читатель выбирает три разные книги из десяти. Сколько существует наборов, если порядок выбора не учитывается?

**Разбор.** Порядок книг в наборе несущественен, поэтому считаем сочетания:

$$
\binom{10}{3}=\frac{10!}{3!7!}=120.
$$

**Ответ.** 120 наборов.

### S02-C05

**Условие.** Сколько трёхзначных чётных чисел можно записать цифрами от 0 до 6, если цифры могут повторяться?

### S02-C06

**Условие.** Код состоит только из символов 4 и 5 и может иметь длину 3, 4 или 5. Сколько разных кодов допустимо?

### S02-C07

**Условие.** В соревновании участвуют 18 человек. Сколькими способами можно распределить золотую, серебряную и бронзовую медали, если один участник не получает две медали?

### S02-C08

**Условие.** Для дежурства выбирают одного сержанта из трёх и трёх солдат из тридцати. Сколько составов дежурной группы возможно?

### S02-C09

**Условие.** На матч выбирают одного вратаря из двух, двух защитников из семи и трёх нападающих из десяти. Сколько стартовых составов возможно?

### S02-C10 — типовая

**Условие.** Три приза распределяют между пятью сотрудниками, причём один сотрудник получает не более одного приза. Сколько распределений возможно, если: а) призы различны; б) призы одинаковы?

**Разбор.** Для разных призов важен их порядок назначения:

$$
A_5^3=5\cdot4\cdot3=60.
$$

Для одинаковых призов важен только набор трёх получателей:

$$
\binom53=10.
$$

**Ответ.** а) 60; б) 10.

### S02-C11

**Условие.** Из 30 сотрудников выбирают двух представителей. Сколько вариантов получится: а) если одного из выбранных назначают старшим; б) если роли одинаковы?

### S02-C12

**Условие.** Сколько диагоналей имеет выпуклый двенадцатиугольник?

### S02-C13

**Условие.** В круговом турнире каждая пара команд сыграла один матч, всего состоялось 153 матча. Сколько команд участвовало?

### S02-C14 — типовая

**Условие.** Комиссию из восьми человек формируют из двух математиков и восьми экономистов. Сколько комиссий содержат хотя бы одного математика?

**Разбор.** Из всех комиссий исключим единственную комиссию без математиков:

$$
\binom{10}{8}-\binom88=45-1=44.
$$

Прямой подсчёт даёт тот же результат:

$$
\binom21\binom87+\binom22\binom86=16+28=44.
$$

**Ответ.** 44 комиссии.

### S02-C15

**Условие.** Из 15 рабочих выбирают бригадира и ещё четырёх членов бригады. Сколько различных составов с назначенным бригадиром возможно?

## 2. Классическое определение вероятности — 25 задач

### S02-P01 — типовая

**Условие.** В контейнере 3 белых и 7 чёрных датчиков. Один датчик выбирают равновероятно. Найдите вероятности выбрать белый и чёрный датчик.

**Разбор.** Равновозможны 10 отдельных датчиков. Для белого датчика благоприятны 3 исхода, для чёрного — 7:

$$
P(W)=\frac3{10},\qquad P(B)=\frac7{10}.
$$

Сумма равна 1, поскольку события дополняют друг друга.

**Ответ.** $3/10$ и $7/10$.

### S02-P02

**Условие.** Из пяти разных карточек с буквами А, Б, В, Г, Д случайно без возвращения выкладывают три в ряд. Какова вероятность получить слово «ДВА»?

### S02-P03 — типовая

**Условие.** В партии 6 исправных и 4 неисправных устройства. Случайно выбирают два без возвращения. Найдите вероятности событий: а) оба исправны; б) оба неисправны; в) состояния различны.

**Разбор.** Равновозможны $\binom{10}{2}=45$ неупорядоченных пары. Поэтому

$$
P(II)=\frac{\binom62}{45}=\frac13,
$$

$$
P(NN)=\frac{\binom42}{45}=\frac2{15},
$$

$$
P(IN)=\frac{\binom61\binom41}{45}=\frac8{15}.
$$

Три события образуют разбиение, и их вероятности суммируются до 1.

**Ответ.** а) $1/3$; б) $2/15$; в) $8/15$.

### S02-P04

**Условие.** В коробке 10 белых и 5 чёрных шаров. Случайно извлекают один шар. Найдите вероятности событий: а) шар белый; б) шар чёрный; в) шар зелёный.

### S02-P05

**Условие.** Из стандартной колоды 36 карт случайно выбирают одну. Найдите вероятность получить: а) туза; б) карту достоинства валет, дама, король или туз.

### S02-P06

**Условие.** Из 10 белых и 5 чёрных деталей случайно выбирают две без возвращения. Найдите вероятности событий: а) обе белые; б) обе чёрные; в) цвета различны.

### S02-P07

**Условие.** В урне 5 белых и 4 чёрных шара. Случайно выбирают два без возвращения. Найдите вероятность получить шары: а) одного цвета; б) разных цветов.

### S02-P08

**Условие.** Из 10 часов шесть нуждаются в чистке. Для проверки случайно выбирают пять часов. Какова вероятность, что ровно двое из выбранных нуждаются в чистке?

### S02-P09

**Условие.** В автобусе находятся 4 девушки и 5 юношей. На остановке случайно выходят шесть человек. Какова вероятность, что среди вышедших ровно три девушки и три юноши?

### S02-P10 — типовая

**Условие.** Бросают два честных шестигранных кубика. Какова вероятность, что сумма будет чётной и не превысит 6?

**Разбор.** Равновозможны 36 упорядоченных пар. Подходят суммы 2, 4 и 6, которым соответствуют 1, 3 и 5 пар:

$$
P=\frac{1+3+5}{36}=\frac14.
$$

Сами суммы не равновозможны, поэтому знаменатель равен 36, а не числу возможных сумм.

**Ответ.** $1/4$.

### S02-P11

**Условие.** Бросают два честных шестигранных кубика. Какова вероятность получить сумму 6?

### S02-P12

**Условие.** Пять разных карточек с буквами Т, У, К, Б, Е случайно переставляют. Какова вероятность получить слово «БУКЕТ»?

### S02-P13

**Условие.** Семь карточек несут буквы слова «КОЛОБОК»: К встречается дважды, О — трижды, Л и Б — по одному разу. Карточки случайно выкладывают в ряд. Какова вероятность получить слово «КОЛОБОК»?

### S02-P14

**Условие.** В группе 25 студентов: 5 учатся на «отлично», 12 — на «хорошо», остальные имеют другие результаты. Случайно выбирают одного студента. Какова вероятность, что он отличник или хорошист?

### S02-P15

**Условие.** В коробке 5 красных, 6 жёлтых и 4 зелёных шара. Случайно выбирают один. Какова вероятность, что он жёлтый или зелёный?

### S02-P16

**Условие.** Из колоды 36 карт случайно выбирают одну. Какова вероятность, что это бубновая карта или туз?

### S02-P17

**Условие.** Бросают честный шестигранный кубик. Какова вероятность, что результат кратен 2 или 3?

### S02-P18

**Условие.** Из карточек с буквами слова «ОДЕССА» последовательно выбирают три и записывают по порядку. Какова вероятность получить «САД»: а) если после каждого выбора карточку возвращают; б) если карточки не возвращают?

### S02-P19

**Условие.** Из карточек с буквами слова «КОЛОБОК» последовательно выбирают три и записывают по порядку. Какова вероятность получить «БЛО»: а) с возвращением; б) без возвращения?

### S02-P20

**Условие.** Среди 20 фотографий одна отмечена как контрольная. Случайно выбирают четыре разные фотографии. Какова вероятность, что контрольная войдёт в выборку?

### S02-P21

**Условие.** Из целых чисел от 1 до 100 равновероятно выбирают одно. Какова вероятность, что оно делится на 5?

### S02-P22

**Условие.** Три пассажира независимо и равновероятно выходят на одном из четырёх этажей. Какова вероятность, что никакие два пассажира не выйдут на одном этаже?

### S02-P23 — типовая, прикладная

**Условие.** Балансировщик обслуживает 10 серверов. Два сервера выбираются случайно и изначально загружены на 70%, остальные — на 0%. Сравните два механизма: а) каждый запрос направляется на один из 10 серверов равновероятно; б) Power of Two Choices выбирает два различных сервера равновероятно без возвращения и направляет запрос на менее загруженный. Найдите вероятности того, что первый запрос попадёт на один из двух изначально загруженных серверов, подтвердите их Monte Carlo-симуляцией и визуализируйте рост загрузки в Jupyter Notebook.

**Разбор.** При обычной случайной маршрутизации два из десяти равновозможных серверов имеют начальную загрузку 70%:

$$
P_1=\frac2{10}=\frac15.
$$

Power of Two Choices направит первый запрос на один из этих серверов только тогда, когда выбраны они оба:

$$
P_2=\frac{\binom22}{\binom{10}{2}}=\frac1{45}.
$$

Отношение вероятностей равно 9. Сначала проверим расчёт на 100 000 независимых первых запросов. Ячейки ниже нужно запускать по порядку.

```python
from math import comb
from random import Random

import matplotlib.pyplot as plt
from matplotlib.animation import FuncAnimation
from matplotlib.patches import Patch, Rectangle
from IPython.display import HTML, display

SERVER_COUNT = 10
INITIAL_LOAD = 0.70
REQUEST_LOAD = 0.05
REQUESTS = 140
TRIALS = 100_000
SEED = 2026

initial_servers = set(Random(SEED).sample(range(SERVER_COUNT), 2))
initial_loads = [
    INITIAL_LOAD if server in initial_servers else 0.0
    for server in range(SERVER_COUNT)
]

print("Серверы с начальной загрузкой 70%:", sorted(initial_servers))
```

Если `matplotlib` отсутствует в kernel, перед первой ячейкой выполните `%pip install matplotlib` и перезапустите kernel.

```python
monte_carlo_rng = Random(SEED + 1)
random_hits = 0
power_two_hits = 0

for _ in range(TRIALS):
    random_hits += monte_carlo_rng.randrange(SERVER_COUNT) in initial_servers

    candidates = monte_carlo_rng.sample(range(SERVER_COUNT), 2)
    chosen = min(candidates, key=initial_loads.__getitem__)
    power_two_hits += chosen in initial_servers

p_random = random_hits / TRIALS
p_power_two = power_two_hits / TRIALS
exact_random = len(initial_servers) / SERVER_COUNT
exact_power_two = comb(len(initial_servers), 2) / comb(SERVER_COUNT, 2)

print(f"random:    empirical={p_random:.5f}, exact={exact_random:.5f}")
print(f"power-two: empirical={p_power_two:.5f}, exact={exact_power_two:.5f}")

assert abs(p_random - exact_random) <= 0.005
assert abs(p_power_two - exact_power_two) <= 0.005
```

Для seed 2026 выбираются серверы 1 и 5; частоты равны `0.19812` и `0.02253`, а точные значения — `0.20000` и `0.02222`.

Следующая ячейка заранее вычисляет две истории, а затем показывает их рядом. Один запрос добавляет 5 процентных пунктов; при 100% новый запрос отклоняется.

```python
def simulate_requests(strategy, seed):
    loads = initial_loads.copy()
    history = [loads.copy()]
    dropped_history = [0]
    dropped = 0
    rng = Random(seed)

    for _ in range(REQUESTS):
        if strategy == "random":
            chosen = rng.randrange(SERVER_COUNT)
        elif strategy == "power_two":
            candidates = rng.sample(range(SERVER_COUNT), 2)
            chosen = min(candidates, key=loads.__getitem__)
        else:
            raise ValueError("Unknown strategy")

        if loads[chosen] >= 1.0:
            dropped += 1
        else:
            loads[chosen] = min(1.0, loads[chosen] + REQUEST_LOAD)

        history.append(loads.copy())
        dropped_history.append(dropped)

    return history, dropped_history


random_history, random_dropped = simulate_requests("random", SEED + 2)
power_history, power_dropped = simulate_requests("power_two", SEED + 3)


def load_color(load):
    if load >= 1.0 - 1e-12:
        return "#d62728"  # красный: 100%
    if load >= 0.80 - 1e-12:
        return "#ff7f0e"  # оранжевый: 80–99%
    if load >= 0.50 - 1e-12:
        return "#f2c94c"  # жёлтый: 50–79%
    return "#4c78a8"      # синий: менее 50%


fig, axes = plt.subplots(1, 2, figsize=(13, 5.8))
panels = []

for axis, title in zip(
    axes,
    ("Равномерный случайный выбор", "Power of Two Choices"),
):
    axis.set_xlim(-0.15, 5.0)
    axis.set_ylim(-0.25, 2.25)
    axis.set_aspect("equal")
    axis.axis("off")
    axis.set_title(title, fontsize=13, fontweight="bold")

    fills = []
    percentages = []
    for server in range(SERVER_COUNT):
        x = server % 5
        y = 1 - server // 5
        axis.add_patch(Rectangle((x, y), 0.82, 0.82, fill=False,
                                 edgecolor="#333333", linewidth=1.5))
        fill = Rectangle((x, y), 0.82, 0.0, facecolor="#4c78a8")
        axis.add_patch(fill)
        fills.append(fill)
        percentages.append(
            axis.text(x + 0.41, y + 0.43, "0%", ha="center", va="center",
                      fontsize=10, fontweight="bold")
        )
        axis.text(x + 0.41, y - 0.08, f"S{server}", ha="center", va="top",
                  fontsize=9)

    status = axis.text(2.41, 2.08, "", ha="center", fontsize=10)
    panels.append((fills, percentages, status))

legend = [
    Patch(color="#4c78a8", label="< 50%"),
    Patch(color="#f2c94c", label="50–79%"),
    Patch(color="#ff7f0e", label="80–99%"),
    Patch(color="#d62728", label="100%"),
]
fig.legend(handles=legend, loc="lower center", ncol=4, frameon=False)
heading = fig.suptitle("", fontsize=14)
fig.tight_layout(rect=(0, 0.08, 1, 0.92))


def update(frame):
    heading.set_text(f"Обработано запросов: {frame} из {REQUESTS}")
    states = (
        (random_history[frame], random_dropped[frame]),
        (power_history[frame], power_dropped[frame]),
    )

    for (fills, percentages, status), (loads, dropped) in zip(panels, states):
        for fill, label, load in zip(fills, percentages, loads):
            fill.set_height(0.82 * load)
            fill.set_facecolor(load_color(load))
            label.set_text(f"{load:.0%}")
        status.set_text(f"Отклонено запросов: {dropped}")


animation = FuncAnimation(
    fig,
    update,
    frames=range(REQUESTS + 1),
    interval=120,
    repeat=False,
)
plt.close(fig)
display(HTML(animation.to_jshtml()))
```

При заданных параметрах обычный алгоритм заканчивает с двумя заполненными серверами и 10 отклонёнными запросами; Power of Two Choices не отклоняет запросы и удерживает загрузку серверов в диапазоне 75–90%.

**Ответ.** $P_1=1/5$, $P_2=1/45$.

### S02-P24

**Условие.** Десять разных карточек с буквами слова «ПОРТУГАЛИЯ» случайно переставляют. Какова вероятность получить исходное слово?

### S02-P25

**Условие.** Десять физических карточек с буквами слова «МАДАГАСКАР» случайно переставляют; четыре карточки несут букву А. Какова вероятность, что видимая последовательность совпадёт со словом «МАДАГАСКАР»?
