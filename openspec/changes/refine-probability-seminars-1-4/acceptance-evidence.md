# Итоговая приёмка реализации

Дата проверки: 31.08.2026.

## Approval содержательных исправлений

- В `approval-evidence/` сохранены девять отдельных пакетов.
- Каждый пакет был показан пользователю отдельно и имеет явный статус «одобрено».
- Каждый ограниченный diff применён только после соответствующего ответа пользователя.
- Последний кандидат — описание области $y\ge x$ как расположенной «под диагональю» — исправлен на «над диагональю» в преподавательской и публичной версиях семинара 4.

## Форматирование и рендер

- Явным решением пользователя от 31.08.2026 проверка в Obsidian исключена из итоговой приёмки.
- Две лекции, четыре публичных и четыре преподавательских семинара успешно разобраны Pandoc в режиме `commonmark_x+tex_math_dollars` и преобразованы в HTML5 с MathML без ошибок.
- Во всех десяти файлах каждый настоящий разделитель блочной формулы `$$` находится на отдельной строке в нулевой колонке; пары разделителей сбалансированы.
- Цитатные префиксы, горизонтальные разделители, квадратные служебные метки и указания сложности отсутствуют.
- Из публичного семинара 2 удалена оставшаяся преподавательская пометка «Типичная ошибка»; она сохранена в преподавательской версии.
- В однострочных преподавательских репликах механически исправлены ошибочные закрывающие символы `)` на русскую кавычку `»`; учебное содержание и формулы не изменены.
- Все локальные Markdown-ссылки первых двух лекций и обеих версий семинаров 1–4 разрешаются в существующие файлы.

## Иллюстрации

- Повторная автоматическая проверка подтвердила 9 PNG семинара 4: manifest, PNG-сигнатуры, SHA-256, размеры и publisher-лимиты корректны.
- Публичная и преподавательская версии семинара 4 содержат по 9 уникальных ссылок с содержательными alt-текстами; пропущенных и orphan-файлов нет.
- Треугольник Паскаля расширен панелью «Общие формулы»: внутри общего PNG показаны формула биномиального коэффициента, рекуррентное правило Паскаля и бином Ньютона. Автоматическая проверка подтвердила точные исходные формулы, сигнатуру, SHA-256, размеры, publisher-лимиты, биномиальные коэффициенты, правило суммы соседей и обе ссылки из версий семинара 2.
- Исходный PNG, обе области при масштабе 100% и полный рисунок в оттенках серого просмотрены вручную: общий фон различим, обрезки, наложений, битых глифов, ошибочных чисел и нечитаемых подписей нет.

## Штатный CI

- `npm run lint` — успешно.
- `npm test` — 54 теста из 54 успешны, пропусков и падений нет.
- `npm run validate` выполнен с отдельным временным Git-index, включающим весь целевой diff как tracked-снимок. Основной пользовательский index не менялся.
- Publisher принял вложенные пути `lectures-teacher/seminars/` и распознал новые семинары и PNG как dormant, поскольку `course.yaml` их не объявляет.
- После интеграции актуальной `origin/courses/probability-theory` digest полного снимка и контрольного снимка без новых teacher/asset/OpenSpec-файлов одинаков: `657f8c2ab547bbfd5b39a81492bf4832ab847fd0f726b3b94377ee6ff904cffe`. Следовательно, dormant-ресурсы не меняют публикационный план; изменение digest относительно более раннего запуска вызвано обновлением объявленной лекции 1 в основной ветке курса.

## Ограничение итогового diff

- SHA-256 `course.yaml` совпадает с baseline: `dd75032706b75af5aca04d6ee71ea52bb790750784d5c1aeff241d0ee0833cba`.
- `.github/**`, `README.md`, `course.yaml.example`, лекции 3–8 и `sources/` не изменены.
- SHA-256 содержимого каждого переименованного семинара 5–17 совпадает с baseline.
- Несвязанные `.agents/` и существовавший до реализации `tmp/` сохранены; созданные для приёмки временные HTML удалены.

## Итог

Все проверки, оставшиеся в change после последовательного approval математических исправлений, выполнены. Исключение составляет только Obsidian-просмотр, явно снятый пользователем; вместо него выполнены CommonMark/Pandoc-разбор и структурные проверки разметки.
