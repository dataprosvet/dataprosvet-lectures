# Design samples r1 — pending instructor approval

The user rejected SlidesGPT and requested a local minimalist PPTX prototype before full production. Nine slides cover topic dividers, editable HTTP anatomy, key → value method explanations, HTTP 5xx cats, three cumulative reveal frames, and JSON with explanatory text.

16:9; Arial; background #F0EEE8, ink #22343C, accent #287B79, divider #20383F. Global topic is always the title; current subtopic is the subtitle. Timing is only in speaker notes. No transitions or animations. Existing cats are embedded PNG; text and diagrams are native editable objects. Full lecture slide count will grow beyond the original 37 semantic slides because of dividers and cumulative reveals; total lecture time stays 90 minutes.

All nine rendered slides visually inspected. Finalizer passed integrity, geometry/font and first-party re-import checks. ZIP check confirmed nine notes, no visible time strings and no animation/transition XML. Not tested in native PowerPoint.

Stop: approve prototype before full deck. After approval, package the agreed template through template-creator for future use, without modifying installed Presentations plugin files. No template has been installed yet.
