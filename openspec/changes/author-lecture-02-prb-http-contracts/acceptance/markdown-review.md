# Проверка комплекта Markdown лекции 2

Дата: 14.09.2026. Этап: после явного аппрува плана, перед аппрувом Markdown.

## Разрешение и выполненная работа

Пользователь: «Я не увидел в плане реализации конспекта лекции и краткого конспекта лекции. Добавь его и можешь продолжать. План лекции я одобряю». Добавлены отдельные полный/краткий студенческие продукты в proposal/design/tasks и numbered capability; одобрение точной редакции 3 зафиксировано в review-status.md. В самом плане изменена только административная отметка статуса; исходный и текущий хэши сохранены.

Созданы три самостоятельных Markdown-файла: сценарий учителя, полный конспект и краткий конспект. Сценарий: 35 слайдов, 90 минут, рекап 3 минуты/2 слайда, методы с шестью минутами обязательного интерактива, форматы 15 минут, OpenAPI/Swagger UI 18 минут. Ссылки Carson/RFC находятся рядом с обеими схемами HTTP/2, Habr включён в самостоятельное чтение. gRPC отделён от версии HTTP и формата Protobuf. Преподавательские указания исключены из студенческих текстов. Презентация и PDF не создавались.

## Демонстрация и точные проверки

В обеих копиях комплекта: Swagger UI 5.32.15, openapi-spec-validator 0.9.0, согласованный шаблон 3.2.1 и авторский contracts/lecture02-openapi.yaml. Локальная страница /lecture-02 получает именно этот YAML. /docs продолжает использовать generated /openapi.json. Три generated-файла воспроизведены генератором из чистой копии побайтно; это не ручная замена версии в JSON. Runtime FastAPI 0.116.1 / Pydantic 2.11.7 сохранён. Полные зависимости закреплены, `pip check`: No broken requirements found.

Вывод валидатора:

```text
OK contracts/lecture02-openapi.yaml: OpenAPI 3.2.1, ссылки, 12 примеров
OK contracts/read-api.json: OpenAPI 3.2.1, ссылки, 0 примеров
OK contracts/bank-offices.json: OpenAPI 3.2.1, ссылки, 0 примеров
OK contracts/office-manager.json: OpenAPI 3.2.1, ссылки, 0 примеров
OK openapi-template.yaml: OpenAPI 3.2.1, ссылки, 1 примеров
```

`pytest` из чистой `/tmp/api-lecture02-research/clean-demo`: **6 passed**. Проверены прежние GET/POST, жизненный цикл HTTP-клиента, соответствие реальных ASGI-ответов авторским схемам, отдельные ошибки FastAPI/мока; сломанные ссылка, пример и прежняя версия документа отклоняются. Два предупреждения deprecated API зависимостей не препятствуют проверке и не объявляются ошибками тестов.

Живой HTTP: **9 live HTTP checks passed**. Отдельно шесть ответов (GET 1/7, days=2/abc, офис 43, POST) сверены со схемами и сохранены в lecture-02-reserve для запасного показа. 24/168 точек подтверждены. В браузере: авторский файл загрузился с badge OAS 3.2; Execute GET 42/1 показал правильный URL, 200, application/json и тело; POST показал Request body и Schema Observation с обязательными office_id/visitors. Последняя проверка UI выполнена после удаления YAML-алиасов из педагогического документа.

## Границы проверки среды

Docker daemon отсутствовал, приложение Docker Desktop не найдено. Docker-сборка, контейнерная сеть и observer/mitmweb этого обновления не прогонялись; их работоспособность не объявляется подтверждённой. Проверен отдельный воспроизводимый Python-режим run_lecture.py из чистой копии с тремя loopback API. В нём OfficeManager ходит непосредственно в Read API. Унаследованный HTTP-прокси вызывал 503; исключение localhost/127.0.0.1/::1 добавлено только в окружение дочерних процессов этого режима. Затем все девять HTTP-проверок прошли. Сам обработчик OfficeManager не изменён.

CORS BankOffices для страницы Read API не расширялся: POST в лекционном UI читается, для выполнения используется curl. Generated-схемы остаются неполными по бизнес-ограничениям, Problem Details остаётся отдельным проектируемым примером. Отдельные HTTP/2/gRPC серверы не добавлялись.

## Содержательная и структурная проверка

- 35 последовательных слайдов, 5 400 секунд без зазоров/пересечений; перерыв после 45:00 не входит в время.
- 139 локальных ссылок проверены до добавления этого отчёта и финальных ссылок ревью; новые ссылки ревью сверены повторно.
- 25 JSON/XML/YAML/CSV блоков синтаксически разобраны. Данные XML/CSV согласованы с авторским JSON-набором; null/отсутствие трактуются по объявленному соглашению. Content-Length учебного POST проверен: 30 байт.
- Краткий конспект существенно короче полного; внутренние ссылки/подсказки учителю в студенческих файлах отсутствуют.
- 35 защищённых файлов исходного снимка сохранили хэши: материалы первой лекции, C4, бизнес-код, данные, Compose, runtime-зависимости, исходные тесты и публикационный manifest. Преподавательский текст семинара 2 получил только согласование версии OAS.
- 42 файла двух копий комплекта совпадают. Корпус PDF не менялся; исходные SHA-256 повторно совпали. Manifest обновлён для новых редакторских примечаний.
- OpenSpec strict: **12 passed, 0 failed**. Scoped git diff --check: PASS. Publisher validate на courses/api-technologies: PASS; публикация не запускалась, course.yaml не изменён.

Машиночитаемые хэши продуктов и результаты: [markdown-qa.json](markdown-qa.json). Исходный снимок: [before-apply.json](before-apply.json).

## Следующий этап

Задачи 1–3 и 4.1–4.8 выполнены: **22/33**. Задача 4.9 — ревью сценария и двух конспектов — открыта. Только после явного аппрува преподавательского Markdown разрешён этап презентации; затем отдельный аппрув презентации и подробная PDF. Change остаётся открытым. Коммиты, push/merge, публикация и архивирование не выполнялись.

Python фактической проверки: `3.12.14`.
